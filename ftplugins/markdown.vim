" Open table of contents (require markdown plugin)
nnoremap <localleader>mt :Toc<CR>

" Format the table (cursor must be within table) (require markdown plugin)
nnoremap <localleader>mf :TableFormat<CR><CR>
