let g:sneak#use_ic_scs = 1
let g:sneak#prompt = 'sneak: '
let g:sneak#label = 1

" Note: Unfortunately this must be within the start package as the options do
" not get applied anymore if they are in the optional package part.
