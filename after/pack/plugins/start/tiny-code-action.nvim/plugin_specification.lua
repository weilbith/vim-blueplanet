require('packer').use({ 'rachartier/tiny-code-action.nvim', opt = true })

-- Dependencies
require('packer').use({ 'nvim-lua/plenary.nvim', opt = true })
require('packer').use({ 'nvim-telescope/telescope.nvim', opt = true })
