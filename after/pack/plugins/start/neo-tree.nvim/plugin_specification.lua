require('packer').use({ 'nvim-neo-tree/neo-tree.nvim', opt = true })

-- Dependencies
require('packer').use({ 'nvim-lua/plenary.nvim', opt = true })
require('packer').use({ 'kyazdani42/nvim-web-devicons', opt = true })
require('packer').use({ 'MunifTanjim/nui.nvim', opt = true })
require('packer').use({ 'antosha417/nvim-lsp-file-operations', opt = true })
